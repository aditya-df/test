version: "3"

services:
  knowgenai:
    build:
      context: ../../
      dockerfile: docker/production/Dockerfile
    image: ffadlymii/knowgenai
    restart: always
    ports:
      - "3001:443"
    extra_hosts:
      - "knowgen-ai.metrodata.web.id:0.0.0.0"